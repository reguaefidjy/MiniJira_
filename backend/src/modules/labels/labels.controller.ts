import { RequestHandler } from 'express';

export const list:   RequestHandler = async (_req, _res, next) => next();
export const create: RequestHandler = async (_req, _res, next) => next();
export const update: RequestHandler = async (_req, _res, next) => next();
export const remove: RequestHandler = async (_req, _res, next) => next();
