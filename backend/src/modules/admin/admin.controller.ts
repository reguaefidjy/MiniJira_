import { RequestHandler } from 'express';

export const createUser:     RequestHandler = async (_req, _res, next) => next();
export const updateUser:     RequestHandler = async (_req, _res, next) => next();
export const deactivateUser: RequestHandler = async (_req, _res, next) => next();
